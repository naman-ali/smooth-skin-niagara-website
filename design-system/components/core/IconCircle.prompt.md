Thin olive-stroke circular container for a Lucide icon — echoes the logo's brushstroke ring. Use for feature/benefit lists.

```jsx
<IconCircle icon={<ShieldCheck size={20} strokeWidth={1.5}/>} />
```
